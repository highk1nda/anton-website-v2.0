/**
 * Transition — a short visual breath between the vibrant Hero gradient
 * and the playful Easter Egg section. Pure dark space; no interactive content.
 */
export default function Transition() {
  return <section aria-hidden className="h-[28vh] bg-[#0f0f0f]" />;
}
